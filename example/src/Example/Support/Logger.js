'use strict';

/**
 * Class Logger
 *
 * A simple logger service
 */
class Logger
{
    /**
     * Log a message
     *
     * @param message
     */
    log (message)
    {
        console.log(message);
    }
}

module.exports = Logger;
